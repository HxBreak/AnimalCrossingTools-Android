<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:id="@+id/houseware_image"
        android:layout_width="96dp"
        android:layout_height="96dp"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/houseware_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
        app:layout_constraintEnd_toEndOf="@id/houseware_image"
        app:layout_constraintStart_toStartOf="@id/houseware_image"
        app:layout_constraintTop_toBottomOf="@id/houseware_image"
        tools:text="Name" />
</androidx.constraintlayout.widget.ConstraintLayout>